pdfplumber
python-pptx
python-docx
# --- PDF page render + figure crop/embed (required for image-rich ingest) ---
PyMuPDF
Pillow
# --- OCR for the image bank (optional; PaddleOCR primary on GPU, Tesseract fallback) ---
# GPU OCR (recommended): install paddlepaddle-gpu from the Paddle CUDA index FIRST, then paddleocr:
#   pip install paddlepaddle-gpu -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#   pip install paddleocr
# (NOTE: do not put paddleocr on a plain `pip install -r` line — it would pull CPU paddlepaddle.)
# Fallback OCR: pytesseract + the Tesseract binary (UB-Mannheim build on Windows).
pytesseract
